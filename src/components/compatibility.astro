---
import CompatibilityList from "~/components/compatibility-list.astro";
import ContentSection from "~/components/content-section.astro";
import type { CompatibilityItem } from "~/types";

const personas = [
  {
    name: "Juan",
    description:
      "ResumIA ha revolucionado la forma en que organizo mis ideas después de las reuniones. En lugar de perder tiempo escribiendo todo, simplemente envío una nota de voz y en segundos tengo un resumen claro y estructurado. ¡Increíble!",
  },
  {
    name: "Maria",
    description:
      "Utilizo ResumIA todos los días para tomar notas rápidas. Es como tener un asistente personal que organiza mis pensamientos. No podría estar más contento con el servicio",
  },
  {
    name: "Pedro",
    description:
      "Con ResumIA, ya no tengo miedo de olvidar una buena idea. Grabo un audio y recibo un resumen que puedo guardar fácilmente. Me ha ayudado mucho en mi trabajo",
  },
];
---

<ContentSection title="Testimonios" id="compatibility">
  <div class="flex flex-wrap justify-center gap-6">
    {
      personas.map(({ name, description }) => (
        <div class="flex flex-col items-center w-full sm:w-1/2 lg:w-1/3">
          <div class="flex flex-col items-center gap-4 border border-default bg-offset p-6 rounded-lg h-full">
            <h3 class="name">{name}</h3>
            <p class="description">{description}</p>
          </div>
        </div>
      ))
    }
  </div>
</ContentSection>